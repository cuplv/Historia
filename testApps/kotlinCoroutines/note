This is an example of kotlin's version of AsyncTask.  I downloaded it to see what it is doing under the hood.

The coroutine basically gets compiled into something that looks like a lifecycle aware AsyncTask.
